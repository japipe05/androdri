services:
  backend:
    build:
      context: ./Backend
    container_name: androdri-backend

    ports:
      - "8000:8000"
    environment:
      FRONTEND_ORIGIN: "http://androdri-frontend:3001"
      SMTP_HOST: "smtp.gmail.com"
      SMTP_PORT: "587"
      SMTP_USER: "felipehuchija@gmail.com"
      SMTP_PASSWORD: "yhjmdvmcvqidtxep"
      RECEIVER_EMAIL: "felipehuchija@gmail.com"
    networks:
      - androdri-network

  frontend:
    build:
      context: ./frontend
    container_name: androdri-frontend

    ports:
      - "3001:3000"
    networks:
      - androdri-network
    environment:
      - NEXT_PUBLIC_CONTACT_API_URL=http://androdri-backend:8000/api/contact

networks:
  androdri-network:
    driver: bridge
