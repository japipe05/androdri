import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  output: "standalone",
  // Puedes agregar más opciones si necesitas
  // reactStrictMode: true,
  // swcMinify: true,
};

export default nextConfig;
